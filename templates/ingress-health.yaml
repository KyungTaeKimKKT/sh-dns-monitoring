{{- if .Values.ingressHealth.enabled }}
apiVersion: v1
kind: Pod
metadata:
  name: ingress-health
  namespace: {{ .Release.Namespace }}
  labels:
    app: ingress-health
spec:
  containers:
    - name: echo
      image: hashicorp/http-echo
      args:
        - "-text={\"health\": true}"
      ports:
        - containerPort: 5678
---
apiVersion: v1
kind: Service
metadata:
  name: ingress-health
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: ingress-health
  ports:
    - port: 80
      targetPort: 5678
{{- end }}