apiVersion: v1
kind: Service
metadata:
  name: {{ include "dns-monitoring.fullname" . }}
  labels:
    {{- include "dns-monitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }} # http
      protocol: TCP
      name: http
  selector:
    {{- include "dns-monitoring.selectorLabels" . | nindent 4 }}
